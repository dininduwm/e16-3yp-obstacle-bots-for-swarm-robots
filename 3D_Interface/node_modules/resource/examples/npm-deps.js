var resource = require('../'),
    faker = resource.define('faker');

faker.dependencies = {
  'Faker': '*'
};

faker.method('start', function start (cb){ 
  console.log('started');
  cb(null, true);
});

faker.start(function(){
  console.log('after started')
})
